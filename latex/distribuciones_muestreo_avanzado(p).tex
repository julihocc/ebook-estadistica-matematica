% ==============================================================================
% SECCIÓN 05.01: MUESTREO ALEATORIO SIMPLE, MEDIA Y VARIANZA MUESTRAL INSESGADA
% ==============================================================================

\subsection*{Enunciados de los problemas --- Sección 05.01}

\subsubsection*{Nivel Fundamental}

\begin{problema}
	\label{prob:5.1.1}
	Una muestra aleatoria simple de tamaño $n=5$ produce las observaciones $\{8, 12, 10, 14, 6\}$.
	\begin{enumerate}
		\item Calcule la media muestral $\bar{X}$.
		\item Calcule la varianza muestral insesgada $S^{2}$.
	\end{enumerate}
\end{problema}

\begin{problema}
	\label{prob:5.1.2}
	Los puntajes de un examen estandarizado tienen media poblacional $\mu = 500$ y desviación estándar $\sigma = 40$. Se toma una muestra aleatoria simple de $n = 64$ estudiantes.
	\begin{enumerate}
		\item Calcule $E(\bar{X})$.
		\item Calcule $\Var(\bar{X})$ y el error estándar $\text{DE}(\bar{X})$.
	\end{enumerate}
\end{problema}

\begin{problema}
	\label{prob:5.1.3}
	Clasifique cada una de las siguientes cantidades como \emph{estadístico} o \emph{parámetro}, justificando su respuesta: (a) la media poblacional $\mu$; (b) la media muestral $\bar{x}$ de una encuesta; (c) la proporción muestral $\hat{p}$ de una elección; (d) la varianza poblacional $\sigma^{2}$.
\end{problema}

\subsubsection*{Nivel Operativo}

\begin{problema}
	\label{prob:5.1.4}
	Sean $X_{1}, \ldots, X_{n}$ variables aleatorias independientes con la misma varianza $\sigma^{2}$. Partiendo de la propiedad $\Var(\sum_i X_i) = \sum_i \Var(X_i)$ para variables independientes, derive la fórmula $\Var(\bar{X}) = \sigma^{2}/n$.
\end{problema}

\begin{problema}
	\label{prob:5.1.5}
	Un proceso de medición tiene desviación estándar poblacional conocida $\sigma = 15$. Calcule el error estándar $\text{DE}(\bar{X}) = \sigma/\sqrt{n}$ para tamaños de muestra $n = 25$, $n = 100$ y $n = 400$, y verifique que cuadruplicar $n$ reduce el error estándar a la mitad.
\end{problema}

\begin{problema}
	\label{prob:5.1.6}
	Para los datos $\{5, 7, 9, 11, 13\}$ ($n=5$):
	\begin{enumerate}
		\item Calcule la varianza muestral insesgada $S^{2} = \frac{1}{n-1}\sum(X_i-\bar{X})^{2}$.
		\item Calcule el estimador ``natural'' (sesgado) $\hat{\sigma}^{2} = \frac{1}{n}\sum(X_i-\bar{X})^{2}$.
		\item Verifique numéricamente la relación $\hat{\sigma}^{2} = \frac{n-1}{n}S^{2}$.
	\end{enumerate}
\end{problema}

\subsubsection*{Nivel Analítico}

\begin{problema}
	\label{prob:5.1.7}
	Demuestre que $E(S^{2}) = \sigma^{2}$ para $X_{1}, \ldots, X_{n}$ iid con media $\mu$ y varianza $\sigma^{2}$. Justifique explícitamente por qué $\sum_{i=1}^{n}(X_i-\mu) = n(\bar{X}-\mu)$ y cómo se usa esta identidad en la demostración.
\end{problema}

\begin{problema}
	\label{prob:5.1.8}
	Demuestre formalmente que $\Var(\bar{X}) = \sigma^{2}/n$ usando la independencia de $X_{1}, \ldots, X_{n}$, y explique por qué este resultado, junto con la Ley de los Grandes Números, implica que $\bar{X}$ es un \emph{estimador consistente} de $\mu$ (es decir, que $\Var(\bar{X}) \to 0$ cuando $n \to \infty$).
\end{problema}

\subsubsection*{Nivel Desafiante}

\begin{problema}
	\label{prob:5.1.9}
	Demuestre algebraicamente la fórmula de cálculo directo (``fórmula abreviada'') de la varianza muestral: $S^{2} = \frac{1}{n-1}\left[\sum_{i=1}^n X_i^{2} - n\bar{X}^{2}\right]$, partiendo de la definición $S^{2} = \frac{1}{n-1}\sum(X_i-\bar{X})^{2}$. Aplique ambas fórmulas a los datos $\{2, 4, 6, 8, 10\}$ y verifique que producen el mismo resultado.
\end{problema}

\begin{problema}
	\label{prob:5.1.10}
	Una población finita de $N = 200$ cuentas bancarias tiene varianza $\sigma^{2} = 900$. Se extrae una muestra aleatoria simple de $n = 20$ cuentas \emph{sin reemplazo}.
	\begin{enumerate}
		\item Calcule $\Var(\bar{X})$ usando la fórmula ingenua de población infinita $\sigma^{2}/n$.
		\item Calcule $\Var(\bar{X})$ usando el factor de corrección por población finita (FPC): $\Var(\bar{X}) = \frac{\sigma^{2}}{n}\cdot\frac{N-n}{N-1}$.
		\item Explique por qué la FPC siempre reduce la varianza cuando se muestrea sin reemplazo de una población finita.
	\end{enumerate}
\end{problema}

\subsection*{Sugerencias de los problemas --- Sección 05.01}

\begin{sugerencia}
	\label{sug:5.1.1}
	Para el Problema \ref{prob:5.1.1}, $\bar{X} = 50/5 = 10$. Las desviaciones al cuadrado son $4, 4, 0, 16, 16$, con suma $40$; divida entre $n-1=4$.
\end{sugerencia}

\begin{sugerencia}
	\label{sug:5.1.2}
	Para el Problema \ref{prob:5.1.2}, use directamente $E(\bar{X})=\mu$ y $\Var(\bar{X})=\sigma^{2}/n$ con $\sigma^{2}=1600$ y $n=64$.
\end{sugerencia}

\begin{sugerencia}
	\label{sug:5.1.3}
	Para el Problema \ref{prob:5.1.3}, un parámetro describe a la población (fijo, generalmente desconocido); un estadístico se calcula a partir de una muestra observada (variable aleatoria, conocido una vez tomada la muestra).
\end{sugerencia}

\begin{sugerencia}
	\label{sug:5.1.4}
	Para el Problema \ref{prob:5.1.4}, escriba $\bar{X}=\frac{1}{n}\sum X_i$ y use $\Var(aY)=a^{2}\Var(Y)$ junto con la aditividad de la varianza bajo independencia.
\end{sugerencia}

\begin{sugerencia}
	\label{sug:5.1.5}
	Para el Problema \ref{prob:5.1.5}, $\text{DE}(\bar{X})=15/\sqrt{n}$: para $n=25$, $15/5=3$; para $n=100$, $15/10=1.5$; para $n=400$, $15/20=0.75$.
\end{sugerencia}

\begin{sugerencia}
	\label{sug:5.1.6}
	Para el Problema \ref{prob:5.1.6}, $\bar{X}=9$; las desviaciones al cuadrado son $16,4,0,4,16$, con suma $40$. Divida entre $4$ para $S^{2}$ y entre $5$ para $\hat{\sigma}^{2}$.
\end{sugerencia}

\begin{sugerencia}
	\label{sug:5.1.7}
	Para el Problema \ref{prob:5.1.7}, siga la demostración del Teorema de insesgadez de $S^2$: escriba $(X_i-\bar X)=(X_i-\mu)-(\bar X-\mu)$, expanda el cuadrado, sume sobre $i$ y use $\sum(X_i-\mu)=n(\bar X-\mu)$ (que se sigue de la definición de $\bar X$) para simplificar el término cruzado.
\end{sugerencia}

\begin{sugerencia}
	\label{sug:5.1.8}
	Para el Problema \ref{prob:5.1.8}, $\Var(\bar{X})=\Var\left(\frac{1}{n}\sum X_i\right)=\frac{1}{n^{2}}\sum\Var(X_i)=\frac{n\sigma^{2}}{n^{2}}=\sigma^{2}/n \to 0$ cuando $n\to\infty$.
\end{sugerencia}

\begin{sugerencia}
	\label{sug:5.1.9}
	Para el Problema \ref{prob:5.1.9}, expanda $(X_i-\bar X)^2 = X_i^2 - 2X_i\bar X + \bar X^2$, sume sobre $i$, y use $\sum X_i = n\bar X$ para simplificar $-2\bar X\sum X_i + n\bar X^2$ a $-n\bar X^2$.
\end{sugerencia}

\begin{sugerencia}
	\label{sug:5.1.10}
	Para el Problema \ref{prob:5.1.10}, el factor FPC $(N-n)/(N-1) = 180/199 \approx 0.9045$ es siempre menor que $1$ cuando $n>1$ y $N$ es finito, por lo que siempre reduce la varianza ingenua.
\end{sugerencia}

\subsection*{Soluciones de los problemas --- Sección 05.01}

\begin{solucion}
	\label{sol:5.1.1}
	Para el Problema \ref{prob:5.1.1} con datos $\{8, 12, 10, 14, 6\}$:
	\begin{enumerate}
		\item $\bar{X} = \dfrac{8+12+10+14+6}{5} = \dfrac{50}{5} = 10$.
		\item Las desviaciones al cuadrado son $(8-10)^{2}=4$, $(12-10)^{2}=4$, $(10-10)^{2}=0$, $(14-10)^{2}=16$, $(6-10)^{2}=16$, con suma $40$. Por lo tanto, $S^{2} = 40/(5-1) = 10$. \quad \qedhere
	\end{enumerate}
\end{solucion}

\begin{solucion}
	\label{sol:5.1.2}
	Para el Problema \ref{prob:5.1.2} con $\mu=500$, $\sigma=40$, $n=64$:
	\begin{enumerate}
		\item $E(\bar{X}) = \mu = 500$.
		\item $\Var(\bar{X}) = \sigma^{2}/n = 1600/64 = 25$. $\text{DE}(\bar{X}) = \sqrt{25} = 5$. \quad \qedhere
	\end{enumerate}
\end{solucion}

\begin{solucion}
	\label{sol:5.1.3}
	Para el Problema \ref{prob:5.1.3}: (a) $\mu$ es un \textbf{parámetro} (describe a toda la población, es fijo y generalmente desconocido). (b) $\bar{x}$ es un \textbf{estadístico} (se calcula a partir de la muestra observada). (c) $\hat{p}$ es un \textbf{estadístico} (proporción calculada de datos muestrales). (d) $\sigma^{2}$ es un \textbf{parámetro} (describe la dispersión de toda la población). \quad \qedhere
\end{solucion}

\begin{solucion}
	\label{sol:5.1.4}
	Para el Problema \ref{prob:5.1.4}:
	\begin{align*}
		\Var(\bar{X}) = \Var\left(\frac{1}{n}\sum_{i=1}^{n} X_i\right) = \frac{1}{n^{2}}\Var\left(\sum_{i=1}^{n} X_i\right)
		= \frac{1}{n^{2}}\sum_{i=1}^{n}\Var(X_i) = \frac{1}{n^{2}}\cdot n\sigma^{2} = \frac{\sigma^{2}}{n}. \quad \qedhere
	\end{align*}
\end{solucion}

\begin{solucion}
	\label{sol:5.1.5}
	Para el Problema \ref{prob:5.1.5} con $\sigma=15$:
	\begin{itemize}
		\item $n=25$: $\text{DE}(\bar{X}) = 15/\sqrt{25} = 15/5 = 3$.
		\item $n=100$: $\text{DE}(\bar{X}) = 15/\sqrt{100} = 15/10 = 1.5$.
		\item $n=400$: $\text{DE}(\bar{X}) = 15/\sqrt{400} = 15/20 = 0.75$.
	\end{itemize}
	Cuadruplicar $n$ ($25\to100$, $100\to400$) reduce el error estándar exactamente a la mitad ($3\to1.5$, $1.5\to0.75$), consistente con la dependencia $1/\sqrt{n}$. \quad \qedhere
\end{solucion}

\begin{solucion}
	\label{sol:5.1.6}
	Para el Problema \ref{prob:5.1.6} con datos $\{5,7,9,11,13\}$, $\bar{X}=9$, y desviaciones al cuadrado $16,4,0,4,16$ (suma $40$):
	\begin{enumerate}
		\item $S^{2} = 40/(5-1) = 10$.
		\item $\hat{\sigma}^{2} = 40/5 = 8$.
		\item Verificación: $\frac{n-1}{n}S^{2} = \frac{4}{5}\cdot 10 = 8 = \hat{\sigma}^{2}$. \quad \qedhere
	\end{enumerate}
\end{solucion}

\begin{solucion}
	\label{sol:5.1.7}
	Para el Problema \ref{prob:5.1.7}: como $\bar{X}=\frac{1}{n}\sum X_i$, se sigue que $\sum_{i=1}^n(X_i-\mu) = \sum X_i - n\mu = n\bar{X}-n\mu = n(\bar{X}-\mu)$. Usando esta identidad,
	\begin{align*}
		\sum_{i=1}^n(X_i-\bar X)^2 &= \sum_{i=1}^n\left[(X_i-\mu)-(\bar X-\mu)\right]^2 \\
		&= \sum_{i=1}^n(X_i-\mu)^2 - 2(\bar X-\mu)\underbrace{\sum_{i=1}^n(X_i-\mu)}_{=n(\bar X-\mu)} + n(\bar X-\mu)^2 \\
		&= \sum_{i=1}^n(X_i-\mu)^2 - n(\bar X-\mu)^2.
	\end{align*}
	Tomando esperanza, $E[(X_i-\mu)^2]=\sigma^2$ y $E[(\bar X-\mu)^2]=\Var(\bar X)=\sigma^2/n$, así que $E\left[\sum(X_i-\bar X)^2\right] = n\sigma^2 - n\cdot\sigma^2/n = (n-1)\sigma^2$. Dividiendo entre $n-1$: $E(S^2)=\sigma^2$. \quad \qedhere
\end{solucion}

\begin{solucion}
	\label{sol:5.1.8}
	Para el Problema \ref{prob:5.1.8}: por independencia de $X_1,\ldots,X_n$,
	\begin{align*}
		\Var(\bar X) = \frac{1}{n^2}\sum_{i=1}^n \Var(X_i) = \frac{n\sigma^2}{n^2} = \frac{\sigma^2}{n}.
	\end{align*}
	Como $\sigma^2$ es finita y fija, $\Var(\bar X) = \sigma^2/n \to 0$ cuando $n\to\infty$. Junto con $E(\bar X)=\mu$ para todo $n$ (insesgadez), esto implica que $\bar X$ converge en probabilidad a $\mu$ (por la desigualdad de Chebyshev), es decir, $\bar X$ es un estimador \emph{consistente} de $\mu$. \quad \qedhere
\end{solucion}

\begin{solucion}
	\label{sol:5.1.9}
	Para el Problema \ref{prob:5.1.9}: expandiendo el cuadrado,
	\begin{align*}
		\sum_{i=1}^n(X_i-\bar X)^2 = \sum_{i=1}^n\left(X_i^2 - 2X_i\bar X + \bar X^2\right)
		= \sum_{i=1}^n X_i^2 - 2\bar X\sum_{i=1}^n X_i + n\bar X^2.
	\end{align*}
	Como $\sum X_i = n\bar X$, el término cruzado es $-2\bar X\cdot n\bar X = -2n\bar X^2$, así que la expresión se reduce a $\sum X_i^2 - 2n\bar X^2 + n\bar X^2 = \sum X_i^2 - n\bar X^2$. Por lo tanto $S^2 = \frac{1}{n-1}\left[\sum X_i^2 - n\bar X^2\right]$.

	Para $\{2,4,6,8,10\}$: $\bar X = 30/5=6$. Fórmula directa: desviaciones al cuadrado $16,4,0,4,16$, suma $40$, $S^2=40/4=10$. Fórmula abreviada: $\sum X_i^2 = 4+16+36+64+100=220$, $n\bar X^2 = 5\cdot 36=180$, $S^2=(220-180)/4=40/4=10$. Ambas coinciden. \quad \qedhere
\end{solucion}

\begin{solucion}
	\label{sol:5.1.10}
	Para el Problema \ref{prob:5.1.10} con $N=200$, $\sigma^2=900$, $n=20$:
	\begin{enumerate}
		\item Fórmula ingenua: $\Var(\bar X) = \sigma^2/n = 900/20 = 45$.
		\item Con FPC: $\Var(\bar X) = \dfrac{\sigma^2}{n}\cdot\dfrac{N-n}{N-1} = 45\cdot\dfrac{180}{199} \approx 45\cdot 0.9045 \approx 40.70$.
		\item El factor $(N-n)/(N-1)$ es estrictamente menor que $1$ siempre que $n>1$ y $N$ sea finito (pues $N-n<N-1$ cuando $n>1$), por lo que la FPC siempre reduce la varianza estimada respecto al caso de población infinita; cuando $N\to\infty$ (o $n/N\to 0$), el factor tiende a $1$ y ambas fórmulas coinciden. \quad \qedhere
	\end{enumerate}
\end{solucion}

% ==============================================================================
% SECCIÓN 05.02: TEOREMA DEL LÍMITE CENTRAL ASINTÓTICO
% ==============================================================================

\subsection*{Enunciados de los problemas --- Sección 05.02}

\subsubsection*{Nivel Fundamental}

\begin{problema}
	\label{prob:5.2.1}
	Una población tiene media $\mu=200$ y desviación estándar $\sigma=50$. Se toma una muestra aleatoria simple de $n=100$. Use el TLC para aproximar $P(180 < \bar{X} < 210)$.
\end{problema}

\begin{problema}
	\label{prob:5.2.2}
	Los tiempos de vida de una batería son iid con $\mu=500$ horas y $\sigma=50$ horas. Para una muestra de $n=49$, escriba explícitamente la variable $Z_n$ del Teorema del Límite Central (Lindeberg-Lévy) y establezca su distribución límite.
\end{problema}

\begin{problema}
	\label{prob:5.2.3}
	Una población tiene una distribución fuertemente asimétrica (coeficiente de asimetría grande). ¿Es $n=30$ necesariamente suficiente para una buena aproximación normal de $\bar{X}$? Justifique su respuesta usando el teorema de Berry-Esseen.
\end{problema}

\subsubsection*{Nivel Operativo}

\begin{problema}
	\label{prob:5.2.4}
	Una aseguradora recibe $n=100$ reclamaciones independientes, cada una con media $\mu=\$800$ y desviación estándar $\sigma=\$300$. Use el TLC para aproximar la probabilidad de que el monto total reclamado $T=\sum_{i=1}^{100} X_i$ exceda $\$85{,}000$.
\end{problema}

\begin{problema}
	\label{prob:5.2.5}
	Para una población con $\rho = E|X-\mu|^3 = 200$ y $\sigma=10$, calcule la cota de Berry-Esseen (con $C=0.5$) para $n=25$ y para $n=100$. Verifique que la razón entre ambas cotas es consistente con la tasa de convergencia $O(1/\sqrt{n})$.
\end{problema}

\begin{problema}
	\label{prob:5.2.6}
	En una encuesta, cada respuesta es Bernoulli$(p=0.3)$ independiente. Para una muestra de $n=200$ encuestados, use el TLC para aproximar la distribución de la proporción muestral $\hat{p}=\bar{X}$, y calcule $P(\hat{p} > 0.35)$.
\end{problema}

\subsubsection*{Nivel Analítico}

\begin{problema}
	\label{prob:5.2.7}
	Verifique la demostración del TLC vía FGM específicamente para $X_i \sim \text{Exp}(\lambda)$ (con $\mu=1/\lambda$, $\sigma=1/\lambda$). Use la FGM $M_{X_1}(t) = (1-t/\lambda)^{-1}$ para $t<\lambda$, y muestre que $M_{Z_n}(t) \to e^{t^2/2}$ cuando $n\to\infty$.
\end{problema}

\begin{problema}
	\label{prob:5.2.8}
	Partiendo de la cota de Berry-Esseen $\sup_z|F_{Z_n}(z)-\Phi(z)| \le C\rho/(\sigma^3\sqrt{n})$, derive una fórmula para el tamaño de muestra mínimo $n$ requerido para que el error máximo sea menor a una tolerancia $\varepsilon$. Aplique la fórmula con $C=0.5$, $\rho=150$, $\sigma=5$, $\varepsilon=0.01$.
\end{problema}

\subsubsection*{Nivel Desafiante}

\begin{problema}
	\label{prob:5.2.9}
	Un proceso Bernoulli$(p=0.4)$ se repite $n=250$ veces de forma independiente, y $X=\sum X_i$ es el número total de éxitos. Use el TLC (con corrección de continuidad de Yates) para aproximar $P(X \ge 110)$.
\end{problema}

\begin{problema}
	\label{prob:5.2.10}
	Una población finita de $N=500$ cuentas tiene desviación estándar $\sigma=\$80$. Se extrae una muestra de $n=50$ cuentas \emph{sin reemplazo}. Combine el TLC con la corrección por población finita (FPC, Sección 05.01) para aproximar $P(\bar{X} > \mu + 15)$.
\end{problema}

\subsection*{Sugerencias de los problemas --- Sección 05.02}

\begin{sugerencia}
	\label{sug:5.2.1}
	Para el Problema \ref{prob:5.2.1}, $\text{DE}(\bar X)=50/\sqrt{100}=5$. Estandarice ambos extremos y use la tabla Normal estándar; note que $\Phi(-4)$ es prácticamente $0$.
\end{sugerencia}

\begin{sugerencia}
	\label{sug:5.2.2}
	Para el Problema \ref{prob:5.2.2}, sustituya directamente $\mu=500$, $\sigma=50$, $n=49$ en $Z_n=(\bar X_n-\mu)/(\sigma/\sqrt n)$; recuerde que $\sqrt{49}=7$.
\end{sugerencia}

\begin{sugerencia}
	\label{sug:5.2.3}
	Para el Problema \ref{prob:5.2.3}, la cota de Berry-Esseen depende de $\rho=E|X-\mu|^3$; distribuciones muy asimétricas tienen $\rho$ grande, por lo que requieren $n$ mayores para el mismo nivel de error que una población simétrica con $\rho$ pequeño.
\end{sugerencia}

\begin{sugerencia}
	\label{sug:5.2.4}
	Para el Problema \ref{prob:5.2.4}, use $E(T)=n\mu$ y $\Var(T)=n\sigma^2$ (suma, no media), luego estandarice: $Z=(85000-E(T))/\sqrt{\Var(T)}$.
\end{sugerencia}

\begin{sugerencia}
	\label{sug:5.2.5}
	Para el Problema \ref{prob:5.2.5}, la cota es $0.5\cdot 200/(10^3\sqrt n) = 0.1/\sqrt n$. Evalúe en $n=25$ y $n=100$, y compare la razón con $\sqrt{25/100}=1/2$.
\end{sugerencia}

\begin{sugerencia}
	\label{sug:5.2.6}
	Para el Problema \ref{prob:5.2.6}, $\hat p$ es la media muestral de variables Bernoulli$(0.3)$, con $\Var(X_i)=p(1-p)=0.21$. Use $\text{DE}(\hat p)=\sqrt{0.21/200}$.
\end{sugerencia}

\begin{sugerencia}
	\label{sug:5.2.7}
	Para el Problema \ref{prob:5.2.7}, sustituya $\mu=1/\lambda$, $\sigma=1/\lambda$ en la demostración general; el álgebra es idéntica, solo verifique que los momentos de la exponencial son finitos.
\end{sugerencia}

\begin{sugerencia}
	\label{sug:5.2.8}
	Para el Problema \ref{prob:5.2.8}, despeje $\sqrt n$ de $C\rho/(\sigma^3\sqrt n) < \varepsilon$ para obtener $n > (C\rho/(\sigma^3\varepsilon))^2$.
\end{sugerencia}

\begin{sugerencia}
	\label{sug:5.2.9}
	Para el Problema \ref{prob:5.2.9}, $\mu=np=100$, $\sigma=\sqrt{np(1-p)}=\sqrt{60}\approx 7.746$. Con corrección de Yates, $P(X\ge 110)\approx P(Y\ge 109.5)$.
\end{sugerencia}

\begin{sugerencia}
	\label{sug:5.2.10}
	Para el Problema \ref{prob:5.2.10}, calcule $\Var(\bar X)$ con FPC como en la Sección 05.01, tome raíz cuadrada para obtener $\text{DE}(\bar X)$, y estandarice $Z=15/\text{DE}(\bar X)$.
\end{sugerencia}

\subsection*{Soluciones de los problemas --- Sección 05.02}

\begin{solucion}
	\label{sol:5.2.1}
	Para el Problema \ref{prob:5.2.1} con $\mu=200$, $\sigma=50$, $n=100$: $\text{DE}(\bar X)=50/\sqrt{100}=5$.
	\begin{align*}
		P(180 < \bar X < 210) \approx P\left(\frac{180-200}{5} < Z < \frac{210-200}{5}\right) = P(-4 < Z < 2) = \Phi(2)-\Phi(-4) \approx 0.9772-0.00003 \approx 0.9772. \quad \qedhere
	\end{align*}
\end{solucion}

\begin{solucion}
	\label{sol:5.2.2}
	Para el Problema \ref{prob:5.2.2}: con $\mu=500$, $\sigma=50$, $n=49$ (así $\sqrt{49}=7$):
	\begin{align*}
		Z_{49} = \frac{\bar X_{49} - 500}{50/7} \xrightarrow{d} N(0,1). \quad \qedhere
	\end{align*}
\end{solucion}

\begin{solucion}
	\label{sol:5.2.3}
	Para el Problema \ref{prob:5.2.3}: no necesariamente. La cota de Berry-Esseen, $C\rho/(\sigma^3\sqrt n)$, depende del tercer momento absoluto $\rho=E|X-\mu|^3$. Una distribución fuertemente asimétrica tiene $\rho$ grande en relación a $\sigma^3$, por lo que el error de la aproximación normal en $n=30$ puede ser considerablemente mayor que para una población simétrica con momentos similares; en esos casos se requiere un $n$ mayor para alcanzar la misma precisión. \quad \qedhere
\end{solucion}

\begin{solucion}
	\label{sol:5.2.4}
	Para el Problema \ref{prob:5.2.4}: $E(T) = n\mu = 100\cdot 800 = 80{,}000$. $\Var(T) = n\sigma^2 = 100\cdot 300^2 = 9{,}000{,}000$, así $\text{DE}(T)=3000$.
	\begin{align*}
		P(T > 85{,}000) \approx P\left(Z > \frac{85{,}000-80{,}000}{3000}\right) = P(Z > 1.667) \approx 1-0.9522 = 0.0478. \quad \qedhere
	\end{align*}
\end{solucion}

\begin{solucion}
	\label{sol:5.2.5}
	Para el Problema \ref{prob:5.2.5}: la cota es $\dfrac{0.5\cdot 200}{10^3\sqrt n} = \dfrac{0.1}{\sqrt n}$.
	\begin{itemize}
		\item $n=25$: cota $= 0.1/5 = 0.02$.
		\item $n=100$: cota $= 0.1/10 = 0.01$.
	\end{itemize}
	La razón $0.01/0.02 = 0.5 = \sqrt{25/100}$, confirmando que cuadruplicar $n$ reduce la cota de error exactamente a la mitad, consistente con la tasa $O(1/\sqrt n)$. \quad \qedhere
\end{solucion}

\begin{solucion}
	\label{sol:5.2.6}
	Para el Problema \ref{prob:5.2.6}: $\Var(X_i) = p(1-p) = 0.3\cdot 0.7 = 0.21$, así $\text{DE}(\hat p) = \sqrt{0.21/200} \approx 0.0324$.
	\begin{align*}
		P(\hat p > 0.35) \approx P\left(Z > \frac{0.35-0.3}{0.0324}\right) = P(Z > 1.543) \approx 1-0.9385 = 0.0615. \quad \qedhere
	\end{align*}
\end{solucion}

\begin{solucion}
	\label{sol:5.2.7}
	Para el Problema \ref{prob:5.2.7}: con $\mu=1/\lambda$, $\sigma=1/\lambda$, la FGM estandarizada es
	\begin{align*}
		M_{Z_n}(t) = e^{-\mu t\sqrt n/\sigma}\left[M_{X_1}\!\left(\frac{t}{\sigma\sqrt n}\right)\right]^n
		= e^{-t\sqrt n}\left[\left(1-\frac{t}{\lambda\sigma\sqrt n}\right)^{-1}\right]^n.
	\end{align*}
	Como $\sigma=1/\lambda$, $\lambda\sigma=1$, así que el término interno es $(1-t/\sqrt n)^{-1}$. Tomando logaritmo y expandiendo en serie de Taylor $-\log(1-t/\sqrt n) = t/\sqrt n + t^2/(2n) + O(n^{-3/2})$:
	\begin{align*}
		\log M_{Z_n}(t) = -t\sqrt n + n\left[\frac{t}{\sqrt n}+\frac{t^2}{2n}+O(n^{-3/2})\right] = \frac{t^2}{2}+O(n^{-1/2}) \longrightarrow \frac{t^2}{2},
	\end{align*}
	confirmando $M_{Z_n}(t)\to e^{t^2/2}$, la FGM de $N(0,1)$. \quad \qedhere
\end{solucion}

\begin{solucion}
	\label{sol:5.2.8}
	Para el Problema \ref{prob:5.2.8}: de $C\rho/(\sigma^3\sqrt n) < \varepsilon$ se obtiene $\sqrt n > C\rho/(\sigma^3\varepsilon)$, es decir $n > \left(\dfrac{C\rho}{\sigma^3\varepsilon}\right)^2$.

	Sustituyendo $C=0.5$, $\rho=150$, $\sigma=5$, $\varepsilon=0.01$:
	\begin{align*}
		\frac{C\rho}{\sigma^3\varepsilon} = \frac{0.5\cdot 150}{125\cdot 0.01} = \frac{75}{1.25} = 60 \implies n > 60^2 = 3600.
	\end{align*}
	Se requieren al menos $n=3601$ observaciones para garantizar un error máximo menor a $0.01$. \quad \qedhere
\end{solucion}

\begin{solucion}
	\label{sol:5.2.9}
	Para el Problema \ref{prob:5.2.9}: $\mu = np = 250\cdot 0.4 = 100$, $\sigma=\sqrt{np(1-p)}=\sqrt{60}\approx 7.746$. Con corrección de Yates:
	\begin{align*}
		P(X\ge 110) \approx P(Y\ge 109.5) = P\left(Z \ge \frac{109.5-100}{7.746}\right) = P(Z\ge 1.226) \approx 1-0.8899 = 0.1101. \quad \qedhere
	\end{align*}
\end{solucion}

\begin{solucion}
	\label{sol:5.2.10}
	Para el Problema \ref{prob:5.2.10} con $N=500$, $\sigma=80$ ($\sigma^2=6400$), $n=50$:
	\begin{align*}
		\Var(\bar X) = \frac{\sigma^2}{n}\cdot\frac{N-n}{N-1} = \frac{6400}{50}\cdot\frac{450}{499} = 128\cdot 0.9018 \approx 115.43, \qquad \text{DE}(\bar X) \approx 10.744.
	\end{align*}
	\begin{align*}
		P(\bar X > \mu+15) \approx P\left(Z > \frac{15}{10.744}\right) = P(Z > 1.396) \approx 1-0.9186 = 0.0814. \quad \qedhere
	\end{align*}
\end{solucion}

% ==============================================================================
% SECCIÓN 05.03: DISTRIBUCIÓN CHI-CUADRADA Y VARIANZA MUESTRAL
% ==============================================================================

\subsection*{Enunciados de los problemas --- Sección 05.03}

\subsubsection*{Nivel Fundamental}

\begin{problema}
	\label{prob:5.3.1}
	Sea $X \sim \chi^2_{15}$. Calcule la media $E(X)$ y la varianza $\Var(X)$.
\end{problema}

\begin{problema}
	\label{prob:5.3.2}
	Sean $Z_1, \ldots, Z_5$ variables aleatorias independientes con $Z_i \sim N(0,1)$. Identifique la distribución de $Y = Z_1^2+\cdots+Z_5^2$, incluyendo sus grados de libertad.
\end{problema}

\begin{problema}
	\label{prob:5.3.3}
	Una muestra de tamaño $n=12$ de una población normal con $\sigma^2=20$ produce una varianza muestral $S^2=25$. Calcule el valor del estadístico $(n-1)S^2/\sigma^2$.
\end{problema}

\subsubsection*{Nivel Operativo}

\begin{problema}
	\label{prob:5.3.4}
	Sean $X \sim \chi^2_5$ y $Y \sim \chi^2_8$ independientes. Use la propiedad de reproductividad para identificar la distribución de $X+Y$, y calcule su media y varianza.
\end{problema}

\begin{problema}
	\label{prob:5.3.5}
	Use la relación $\chi^2_\nu = \text{Gamma}(\nu/2, 2)$ y la MGF de la distribución gamma, $M_{X}(t)=(1-\beta t)^{-\alpha}$ (Sección 04.07), para escribir la MGF de $\chi^2_6$.
\end{problema}

\begin{problema}
	\label{prob:5.3.6}
	Una máquina embotelladora debe tener varianza $\sigma_0^2=10$. Una muestra de $n=16$ botellas produce $S^2=18.2$. Calcule el estadístico $(n-1)S^2/\sigma_0^2$ y decida si se rechaza $H_0: \sigma^2=10$ a nivel $\alpha=0.05$, usando el valor crítico $\chi^2_{15,0.95}\approx 24.996$.
\end{problema}

\subsubsection*{Nivel Analítico}

\begin{problema}
	\label{prob:5.3.7}
	Partiendo de $\chi^2_\nu = \text{Gamma}(\nu/2, 2)$ y de las fórmulas generales $\mu_X=\alpha\beta$, $\sigma_X^2=\alpha\beta^2$ para $X\sim\text{Gamma}(\alpha,\beta)$ (Sección 04.07), derive algebraicamente que $E(\chi^2_\nu)=\nu$ y $\Var(\chi^2_\nu)=2\nu$.
\end{problema}

\begin{problema}
	\label{prob:5.3.8}
	Demuestre la propiedad de reproductividad de la chi-cuadrada usando funciones generadoras de momentos: si $X\sim\chi^2_{\nu_1}$ y $Y\sim\chi^2_{\nu_2}$ son independientes, entonces $X+Y\sim\chi^2_{\nu_1+\nu_2}$.
\end{problema}

\subsubsection*{Nivel Desafiante}

\begin{problema}
	\label{prob:5.3.9}
	Sea $X_1,\ldots,X_n$ una muestra normal iid con media $\mu$ y varianza $\sigma^2$. Usando el Teorema de Fisher (independencia de $\bar X$ y $S^2$, y $(n-1)S^2/\sigma^2\sim\chi^2_{n-1}$), muestre algebraicamente que
	\begin{align*}
		T = \frac{\bar X-\mu}{S/\sqrt n} = \frac{Z}{\sqrt{\chi^2_{n-1}/(n-1)}}, \qquad \text{donde } Z=\frac{\bar X-\mu}{\sigma/\sqrt n} \sim N(0,1).
	\end{align*}
	¿Por qué esta descomposición es relevante cuando $\sigma$ es desconocida?
\end{problema}

\begin{problema}
	\label{prob:5.3.10}
	Una muestra de $n=20$ observaciones normales produce $S^2=45$. Construya un intervalo de confianza al $95\%$ para $\sigma^2$ usando los cuantiles $\chi^2_{19,0.025}\approx 8.907$ y $\chi^2_{19,0.975}\approx 32.852$, con la fórmula
	\begin{align*}
		\left[\frac{(n-1)S^2}{\chi^2_{n-1,0.975}},\; \frac{(n-1)S^2}{\chi^2_{n-1,0.025}}\right].
	\end{align*}
\end{problema}

\subsection*{Sugerencias de los problemas --- Sección 05.03}

\begin{sugerencia}
	\label{sug:5.3.1}
	Para el Problema \ref{prob:5.3.1}, use directamente $E(\chi^2_\nu)=\nu$ y $\Var(\chi^2_\nu)=2\nu$ con $\nu=15$.
\end{sugerencia}

\begin{sugerencia}
	\label{sug:5.3.2}
	Para el Problema \ref{prob:5.3.2}, aplique directamente la definición $\chi^2_\nu=\sum_{i=1}^\nu Z_i^2$ con $\nu=5$.
\end{sugerencia}

\begin{sugerencia}
	\label{sug:5.3.3}
	Para el Problema \ref{prob:5.3.3}, sustituya $n=12$, $S^2=25$, $\sigma^2=20$ en $(n-1)S^2/\sigma^2$.
\end{sugerencia}

\begin{sugerencia}
	\label{sug:5.3.4}
	Para el Problema \ref{prob:5.3.4}, use $\chi^2_{\nu_1}+\chi^2_{\nu_2}\sim\chi^2_{\nu_1+\nu_2}$ con $\nu_1=5,\nu_2=8$, y luego aplique $E=\nu$, $\Var=2\nu$ al resultado.
\end{sugerencia}

\begin{sugerencia}
	\label{sug:5.3.5}
	Para el Problema \ref{prob:5.3.5}, sustituya $\alpha=\nu/2=3$, $\beta=2$ en $M_X(t)=(1-\beta t)^{-\alpha}$.
\end{sugerencia}

\begin{sugerencia}
	\label{sug:5.3.6}
	Para el Problema \ref{prob:5.3.6}, calcule $(n-1)S^2/\sigma_0^2 = 15\cdot 18.2/10$ y compare contra $24.996$.
\end{sugerencia}

\begin{sugerencia}
	\label{sug:5.3.7}
	Para el Problema \ref{prob:5.3.7}, sustituya $\alpha=\nu/2$, $\beta=2$ directamente en $\mu_X=\alpha\beta$ y $\sigma_X^2=\alpha\beta^2$.
\end{sugerencia}

\begin{sugerencia}
	\label{sug:5.3.8}
	Para el Problema \ref{prob:5.3.8}, use $M_{X+Y}(t)=M_X(t)M_Y(t)$ para variables independientes, sustituya las MGFs de $\chi^2_{\nu_1}$ y $\chi^2_{\nu_2}$, y reconozca el resultado como la MGF de $\chi^2_{\nu_1+\nu_2}$.
\end{sugerencia}

\begin{sugerencia}
	\label{sug:5.3.9}
	Para el Problema \ref{prob:5.3.9}, divida numerador y denominador de $T$ entre $\sigma/\sqrt n$, y reconozca $S^2/\sigma^2 = \chi^2_{n-1}/(n-1)$ del Teorema de Fisher.
\end{sugerencia}

\begin{sugerencia}
	\label{sug:5.3.10}
	Para el Problema \ref{prob:5.3.10}, sustituya $n=20$, $S^2=45$ directamente en la fórmula del intervalo dada.
\end{sugerencia}

\subsection*{Soluciones de los problemas --- Sección 05.03}

\begin{solucion}
	\label{sol:5.3.1}
	Para el Problema \ref{prob:5.3.1} con $\nu=15$: $E(X)=15$, $\Var(X)=2\cdot 15=30$. \quad \qedhere
\end{solucion}

\begin{solucion}
	\label{sol:5.3.2}
	Para el Problema \ref{prob:5.3.2}: por definición, $Y=\sum_{i=1}^5 Z_i^2 \sim \chi^2_5$ (chi-cuadrada con $5$ grados de libertad). \quad \qedhere
\end{solucion}

\begin{solucion}
	\label{sol:5.3.3}
	Para el Problema \ref{prob:5.3.3}: $(n-1)S^2/\sigma^2 = 11\cdot 25/20 = 275/20 = 13.75$. \quad \qedhere
\end{solucion}

\begin{solucion}
	\label{sol:5.3.4}
	Para el Problema \ref{prob:5.3.4}: $X+Y \sim \chi^2_{5+8} = \chi^2_{13}$. $E(X+Y)=13$, $\Var(X+Y)=2\cdot 13=26$. \quad \qedhere
\end{solucion}

\begin{solucion}
	\label{sol:5.3.5}
	Para el Problema \ref{prob:5.3.5}: con $\alpha=\nu/2=3$, $\beta=2$: $M_{\chi^2_6}(t) = (1-2t)^{-3}$. \quad \qedhere
\end{solucion}

\begin{solucion}
	\label{sol:5.3.6}
	Para el Problema \ref{prob:5.3.6}: el estadístico es $(n-1)S^2/\sigma_0^2 = 15\cdot 18.2/10 = 27.3$. Como $27.3 > 24.996 = \chi^2_{15,0.95}$, se \textbf{rechaza} $H_0:\sigma^2=10$ a nivel $\alpha=0.05$: hay evidencia de que la varianza real es mayor que $10$. \quad \qedhere
\end{solucion}

\begin{solucion}
	\label{sol:5.3.7}
	Para el Problema \ref{prob:5.3.7}: sustituyendo $\alpha=\nu/2$, $\beta=2$:
	\begin{align*}
		E(\chi^2_\nu) = \alpha\beta = \frac{\nu}{2}\cdot 2 = \nu, \qquad
		\Var(\chi^2_\nu) = \alpha\beta^2 = \frac{\nu}{2}\cdot 4 = 2\nu. \quad \qedhere
	\end{align*}
\end{solucion}

\begin{solucion}
	\label{sol:5.3.8}
	Para el Problema \ref{prob:5.3.8}: por independencia,
	\begin{align*}
		M_{X+Y}(t) = M_X(t)M_Y(t) = (1-2t)^{-\nu_1/2}(1-2t)^{-\nu_2/2} = (1-2t)^{-(\nu_1+\nu_2)/2},
	\end{align*}
	que es exactamente la MGF de $\chi^2_{\nu_1+\nu_2}$. Por el teorema de unicidad de la MGF, $X+Y\sim\chi^2_{\nu_1+\nu_2}$. \quad \qedhere
\end{solucion}

\begin{solucion}
	\label{sol:5.3.9}
	Para el Problema \ref{prob:5.3.9}: dividiendo numerador y denominador de $T$ entre $\sigma/\sqrt n$,
	\begin{align*}
		T = \frac{\bar X-\mu}{S/\sqrt n} = \frac{(\bar X-\mu)/(\sigma/\sqrt n)}{S/\sigma} = \frac{Z}{\sqrt{S^2/\sigma^2}} = \frac{Z}{\sqrt{\chi^2_{n-1}/(n-1)}},
	\end{align*}
	usando que $(n-1)S^2/\sigma^2\sim\chi^2_{n-1}$ (Teorema de Fisher). Esta descomposición es relevante porque, cuando $\sigma$ es desconocida y se reemplaza por $S$, el estadístico resultante $T$ ya no sigue una distribución Normal exacta, sino la distribución $t$ de Student (Sección 05.04), definida precisamente como el cociente de una Normal estándar entre la raíz de una chi-cuadrada independiente dividida entre sus grados de libertad. \quad \qedhere
\end{solucion}

\begin{solucion}
	\label{sol:5.3.10}
	Para el Problema \ref{prob:5.3.10} con $n=20$, $S^2=45$, $(n-1)S^2 = 19\cdot 45 = 855$:
	\begin{align*}
		\left[\frac{855}{32.852},\; \frac{855}{8.907}\right] \approx [26.03,\; 95.99].
	\end{align*}
	Con $95\%$ de confianza, la varianza poblacional $\sigma^2$ está entre aproximadamente $26.03$ y $95.99$. \quad \qedhere
\end{solucion}

% ==============================================================================
% SECCIÓN 05.04: DISTRIBUCIÓN T DE STUDENT Y MUESTRAS PEQUEÑAS
% ==============================================================================

\subsection*{Enunciados de los problemas --- Sección 05.04}

\subsubsection*{Nivel Fundamental}

\begin{problema}
	\label{prob:5.4.1}
	Sea $T \sim t_{10}$. Calcule $\Var(T)$.
\end{problema}

\begin{problema}
	\label{prob:5.4.2}
	Una muestra de $n=16$ observaciones normales produce $\bar X=72.4$ y $S=8.5$. Construya un intervalo de confianza del $95\%$ para $\mu$ usando $t_{15,0.025}\approx 2.131$.
\end{problema}

\begin{problema}
	\label{prob:5.4.3}
	Compare el cuantil $t_{5,0.025}\approx 2.571$ con $z_{0.025}=1.96$, y el cuantil $t_{30,0.025}\approx 2.042$ con el mismo $z_{0.025}$. Calcule la diferencia porcentual en cada caso y explique la tendencia observada.
\end{problema}

\subsubsection*{Nivel Operativo}

\begin{problema}
	\label{prob:5.4.4}
	Los datos $\{23, 25, 21, 27, 24\}$ ($n=5$) provienen de una población normal. Calcule $\bar X$ y $S$, y construya un intervalo de confianza del $95\%$ para $\mu$ usando $t_{4,0.025}\approx 2.776$.
\end{problema}

\begin{problema}
	\label{prob:5.4.5}
	Se afirma que la media poblacional es $\mu_0=100$. Una muestra de $n=25$ produce $\bar X=104.2$ y $S=12$. Calcule el estadístico $t=(\bar X-\mu_0)/(S/\sqrt n)$ y decida si se rechaza $H_0:\mu=100$ a nivel $\alpha=0.05$, usando $t_{24,0.025}\approx 2.064$.
\end{problema}

\begin{problema}
	\label{prob:5.4.6}
	Compare los cuantiles $t_{5,0.025}\approx 2.571$, $t_{30,0.025}\approx 2.042$ y $t_{120,0.025}\approx 1.980$ contra $z_{0.025}=1.96$. Describa cómo se comporta la convergencia conforme $\nu$ crece.
\end{problema}

\subsubsection*{Nivel Analítico}

\begin{problema}
	\label{prob:5.4.7}
	Sabiendo que $E(1/\chi^2_\nu) = 1/(\nu-2)$ para $\nu>2$, que $Z$ y $\chi^2_\nu$ son independientes, y que $E(Z^2)=1$, derive $\Var(T)=\nu/(\nu-2)$ para $T=Z/\sqrt{\chi^2_\nu/\nu}$.
\end{problema}

\begin{problema}
	\label{prob:5.4.8}
	Explique, usando la consistencia de $S^2$ (Secciones 05.01/05.03) y el Teorema de Slutsky, por qué $T_n=(\bar X-\mu)/(S/\sqrt n) \xrightarrow{d} N(0,1)$ cuando $n\to\infty$, aun cuando $T_n \sim t_{n-1}$ exactamente para cada $n$ finito.
\end{problema}

\subsubsection*{Nivel Desafiante}

\begin{problema}
	\label{prob:5.4.9}
	En un estudio de pérdida de peso, $n=8$ sujetos se pesan antes y después de una dieta. La diferencia promedio es $\bar d=-3.2$ kg con desviación estándar $S_d=2.1$ kg. Construya un intervalo de confianza del $95\%$ para la diferencia media poblacional, usando $t_{7,0.025}\approx 2.365$, y determine si hay evidencia de que la dieta reduce el peso (es decir, si el intervalo excluye el cero).
\end{problema}

\begin{problema}
	\label{prob:5.4.10}
	Un estudio piloto estima $S\approx 5$. Se desea un margen de error de $E=1.5$ con $95\%$ de confianza. Use la aproximación $n\approx(z_{0.025}S/E)^2$ para obtener una estimación inicial de $n$, y luego verifique, usando $t_{n-1,0.025}$ en lugar de $z_{0.025}$, si el margen de error real alcanzado con ese $n$ sigue siendo aproximadamente $1.5$.
\end{problema}

\subsection*{Sugerencias de los problemas --- Sección 05.04}

\begin{sugerencia}
	\label{sug:5.4.1}
	Para el Problema \ref{prob:5.4.1}, use directamente $\Var(T)=\nu/(\nu-2)$ con $\nu=10$.
\end{sugerencia}

\begin{sugerencia}
	\label{sug:5.4.2}
	Para el Problema \ref{prob:5.4.2}, calcule el error estándar $S/\sqrt n = 8.5/4$, luego el margen $t_{15,0.025}\cdot(S/\sqrt n)$.
\end{sugerencia}

\begin{sugerencia}
	\label{sug:5.4.3}
	Para el Problema \ref{prob:5.4.3}, calcule $(t_\nu-z)/z \times 100\%$ para cada $\nu$; observe que la diferencia decrece al aumentar $\nu$.
\end{sugerencia}

\begin{sugerencia}
	\label{sug:5.4.4}
	Para el Problema \ref{prob:5.4.4}, $\bar X=24$; las desviaciones al cuadrado son $1,1,9,9,0$, suma $20$, $S^2=20/4=5$, $S=\sqrt 5\approx 2.236$. Note que $S/\sqrt n = \sqrt5/\sqrt5=1$.
\end{sugerencia}

\begin{sugerencia}
	\label{sug:5.4.5}
	Para el Problema \ref{prob:5.4.5}, calcule $t=(104.2-100)/(12/5)$ y compare $|t|$ contra $2.064$.
\end{sugerencia}

\begin{sugerencia}
	\label{sug:5.4.6}
	Para el Problema \ref{prob:5.4.6}, calcule la diferencia porcentual de cada cuantil $t$ respecto a $1.96$ y observe que decrece monótonamente con $\nu$.
\end{sugerencia}

\begin{sugerencia}
	\label{sug:5.4.7}
	Para el Problema \ref{prob:5.4.7}, escriba $T^2=\nu Z^2/\chi^2_\nu$, use independencia para factorizar $E(T^2)=\nu E(Z^2)E(1/\chi^2_\nu)$, y recuerde $E(T)=0$ para $\nu>1$.
\end{sugerencia}

\begin{sugerencia}
	\label{sug:5.4.8}
	Para el Problema \ref{prob:5.4.8}, note que $\chi^2_{n-1}/(n-1)=S^2/\sigma^2 \to 1$ en probabilidad por la consistencia de $S^2$; aplique Slutsky a $T_n=Z/\sqrt{\chi^2_{n-1}/(n-1)}$.
\end{sugerencia}

\begin{sugerencia}
	\label{sug:5.4.9}
	Para el Problema \ref{prob:5.4.9}, calcule $\text{SE}=S_d/\sqrt n = 2.1/\sqrt 8$, luego el margen $t_{7,0.025}\cdot\text{SE}$, y sume/reste a $\bar d$.
\end{sugerencia}

\begin{sugerencia}
	\label{sug:5.4.10}
	Para el Problema \ref{prob:5.4.10}, calcule $n_0=(1.96\cdot 5/1.5)^2$, redondee hacia arriba, y luego recalcule el margen con $t_{n_0-1,0.025}\cdot 5/\sqrt{n_0}$.
\end{sugerencia}

\subsection*{Soluciones de los problemas --- Sección 05.04}

\begin{solucion}
	\label{sol:5.4.1}
	Para el Problema \ref{prob:5.4.1} con $\nu=10$: $\Var(T) = 10/(10-2) = 10/8 = 1.25$. \quad \qedhere
\end{solucion}

\begin{solucion}
	\label{sol:5.4.2}
	Para el Problema \ref{prob:5.4.2}: $\text{SE}=8.5/\sqrt{16}=8.5/4=2.125$. Margen $=2.131\cdot 2.125\approx 4.53$.
	\begin{align*}
		\text{IC}_{95\%} = 72.4 \pm 4.53 = [67.87,\; 76.93]. \quad \qedhere
	\end{align*}
\end{solucion}

\begin{solucion}
	\label{sol:5.4.3}
	Para el Problema \ref{prob:5.4.3}: para $\nu=5$, $(2.571-1.96)/1.96 \approx 31.2\%$ mayor. Para $\nu=30$, $(2.042-1.96)/1.96\approx 4.2\%$ mayor. La diferencia relativa entre el cuantil $t$ y el $z$ decrece considerablemente al aumentar los grados de libertad, consistente con $t_\nu\xrightarrow{d}N(0,1)$. \quad \qedhere
\end{solucion}

\begin{solucion}
	\label{sol:5.4.4}
	Para el Problema \ref{prob:5.4.4}: $\bar X = (23+25+21+27+24)/5 = 120/5 = 24$. Desviaciones al cuadrado: $1,1,9,9,0$, suma $20$, $S^2=20/4=5$, $S=\sqrt5\approx 2.236$. $\text{SE}=S/\sqrt5 = \sqrt5/\sqrt5=1$. Margen $=2.776\cdot 1=2.776$.
	\begin{align*}
		\text{IC}_{95\%} = 24 \pm 2.776 = [21.22,\; 26.78]. \quad \qedhere
	\end{align*}
\end{solucion}

\begin{solucion}
	\label{sol:5.4.5}
	Para el Problema \ref{prob:5.4.5}: $t = (104.2-100)/(12/5) = 4.2/2.4 = 1.75$. Como $|1.75| < 2.064$, \textbf{no se rechaza} $H_0:\mu=100$ a nivel $\alpha=0.05$. \quad \qedhere
\end{solucion}

\begin{solucion}
	\label{sol:5.4.6}
	Para el Problema \ref{prob:5.4.6}: las diferencias porcentuales respecto a $z_{0.025}=1.96$ son $31.2\%$ ($\nu=5$), $4.2\%$ ($\nu=30$) y $1.0\%$ ($\nu=120$). La convergencia es mon\'otona decreciente y bastante rápida: para $\nu\ge 30$ la diferencia ya es pequeña, y para $\nu\ge 120$ es prácticamente despreciable, justificando el uso de $Z$ como aproximación razonable en muestras grandes. \quad \qedhere
\end{solucion}

\begin{solucion}
	\label{sol:5.4.7}
	Para el Problema \ref{prob:5.4.7}: como $E(T)=0$ para $\nu>1$, $\Var(T)=E(T^2)$. Escribiendo $T^2 = Z^2/(\chi^2_\nu/\nu) = \nu Z^2/\chi^2_\nu$, y usando la independencia de $Z$ y $\chi^2_\nu$:
	\begin{align*}
		E(T^2) = \nu\, E(Z^2)\, E\!\left(\frac{1}{\chi^2_\nu}\right) = \nu \cdot 1 \cdot \frac{1}{\nu-2} = \frac{\nu}{\nu-2}.
	\end{align*}
	Por lo tanto $\Var(T) = \nu/(\nu-2)$. \quad \qedhere
\end{solucion}

\begin{solucion}
	\label{sol:5.4.8}
	Para el Problema \ref{prob:5.4.8}: por el Teorema de Fisher, $\chi^2_{n-1}/(n-1) = S^2/\sigma^2$. Como $S^2$ es un estimador consistente de $\sigma^2$ (Secciones 05.01 y 05.03), $S^2/\sigma^2 \to 1$ en probabilidad cuando $n\to\infty$. Por el Teorema de Slutsky, si $Z_n \xrightarrow{d} Z\sim N(0,1)$ (aquí $Z_n=Z$ es fijo) y $Y_n\to 1$ en probabilidad, entonces $Z_n/\sqrt{Y_n} \xrightarrow{d} Z$. Aplicando esto a $T_n = Z/\sqrt{\chi^2_{n-1}/(n-1)}$ se obtiene $T_n \xrightarrow{d} N(0,1)$. \quad \qedhere
\end{solucion}

\begin{solucion}
	\label{sol:5.4.9}
	Para el Problema \ref{prob:5.4.9}: $\text{SE} = 2.1/\sqrt8 \approx 0.7425$. Margen $=2.365\cdot 0.7425\approx 1.756$.
	\begin{align*}
		\text{IC}_{95\%} = -3.2 \pm 1.756 = [-4.956,\; -1.444].
	\end{align*}
	Como el intervalo completo es negativo (excluye el cero), hay evidencia estadística de que la dieta reduce el peso promedio. \quad \qedhere
\end{solucion}

\begin{solucion}
	\label{sol:5.4.10}
	Para el Problema \ref{prob:5.4.10}: la estimación inicial es
	\begin{align*}
		n_0 = \left(\frac{1.96\cdot 5}{1.5}\right)^2 = (6.533)^2 \approx 42.7 \implies n_0 = 43.
	\end{align*}
	Con $n_0=43$, $\nu=42$ y $t_{42,0.025}\approx 2.018$ (ligeramente mayor que $1.96$), el margen real alcanzado es
	\begin{align*}
		2.018\cdot\frac{5}{\sqrt{43}} \approx 2.018\cdot 0.7625 \approx 1.539,
	\end{align*}
	ligeramente superior a los $1.5$ deseados. Esto ilustra que, para muestras pequeñas/moderadas, la estimación inicial basada en $z$ subestima levemente el tamaño de muestra necesario, y en la práctica se requiere uno o más pasos de refinamiento con el cuantil $t$ correspondiente. \quad \qedhere
\end{solucion}
