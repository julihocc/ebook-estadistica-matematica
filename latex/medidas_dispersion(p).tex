\section*{Problemas}

\subsection{Enunciados de los problemas}

\subsubsection*{1. Nivel Fundamental (Sencillos / Directos)}

\begin{problema}
	\label{prob:1.4.1}
	Encontrar el rango y las desviaciones media y estándar de los siguientes arreglos de datos no agrupados: 
	\begin{enumerate}
		\item $12, 6, 7, 3, 15, 10, 18, 5$ 
		\item $9, 3, 8, 8, 9, 8, 9, 18.$
	\end{enumerate}
	Compruebe sus resultados con \texttt{Python.}
\end{problema}

\begin{problema}
	\label{prob:1.4.2}
	Encontrar las desviaciones media y estándar de las estaturas de 100 estudiantes que se presentan en la siguiente tabla:
	\begin{figure}[ht]
		\centering
		\includegraphics[width=10cm,keepaspectratio=true]{./images/tab0302.png}
		\label{tab:1.4.1}
	\end{figure}
\end{problema}

\begin{problema}
	\label{prob:1.4.3}
	Encontrar las desviaciones media y estándar de las estaturas de los estudiantes presentadas en la siguiente tabla de frecuencias:
	\begin{figure}[ht]
		\centering
		\includegraphics[height=5cm,keepaspectratio=true]{./images/tab0201.png}
		\label{tab:1.4.2}
	\end{figure}
\end{problema}

\subsubsection*{2. Nivel Operativo (Intermedios / De Desarrollo)}

\begin{problema}
	\label{prob:1.4.5}
	Utilizando la fórmula abreviada para la varianza en datos agrupados, encuentre la desviación estándar de los datos presentados en la tabla \ref{tab:1.4.2}.
\end{problema}

\begin{problema}
	\label{prob:1.4.8}
	Encuentre la media aritmética y la desviación estándar de la siguiente distribución de datos:
	\begin{figure}[ht]
		\centering
		\includegraphics[height=5cm]{./images/tab0205.png}
		\label{tab:1.4.3}
	\end{figure}
\end{problema}

\begin{problema}
	\label{prob:1.4.9}
	Dos grupos de estudiantes tienen las siguientes características muestrales:
	\begin{itemize}
		\item Grupo A: $n_1 = 30$ estudiantes, $\bar{X}_1 = 75$ puntos, $s_1 = 8$ puntos
		\item Grupo B: $n_2 = 20$ estudiantes, $\bar{X}_2 = 75$ puntos, $s_2 = 12$ puntos
	\end{itemize}
	Encuentre la desviación estándar del grupo combinado de $n_1 + n_2 = 50$ estudiantes.
\end{problema}

\subsubsection*{3. Nivel Analítico (Avanzados / De Conexión)}

\begin{problema}
	\label{prob:1.4.4}
	Demostrar rigurosamente que para cualquier conjunto de datos (no agrupados o agrupados):
	\begin{align}
		s &= \sqrt{\dfrac{\sum X^{2}}{N}-\left( \dfrac{\sum X}{N} \right)^{2}} = \sqrt{\overline{X^{2}}-\overline{X}^{2}}\\
		s &= \sqrt{\dfrac{\sum fX^{2}}{N}-\left( \dfrac{\sum fX}{N} \right)^{2}} = \sqrt{\overline{X^{2}}-\overline{X}^{2}}
	\end{align}
\end{problema}

\begin{problema}
	\label{prob:1.4.6}
	Si $d=X-P$ representan las desviaciones de una variable $X$ respecto a un pivote constante $P,$ demostrar que la desviación estándar es invariante ante traslaciones e igual a:
	\begin{align}
		s=\sqrt{\dfrac{\sum fd^{2}}{N}-\left( \dfrac{\sum fd}{N} \right)^{2}}.
	\end{align}
\end{problema}

\subsubsection*{4. Nivel Desafiante (Expertos / Deducción Profunda)}

\begin{problema}
	\label{prob:1.4.7}
	Utilizando la fórmula con pivote y cambio de escala $u_{i} = \dfrac{X_{i}-P}{c}$, demuestre que $s = c \cdot s_{u}$ y aplique este método para calcular la desviación estándar de los datos en la tabla \ref{tab:1.4.2}.
\end{problema}

\begin{problema}
	\label{prob:1.4.10}
	El coeficiente de variación se define como:
	\begin{align}
		CV = \frac{s}{\bar{X}} \times 100\%
	\end{align}
	Compare e interprete la variabilidad relativa en las siguientes dos situaciones prácticas:
	\begin{itemize}
		\item Los salarios de dos empresas: Empresa A tiene $\bar{X}_A = \$45,000$ y $s_A = \$8,000$; Empresa B tiene $\bar{X}_B = \$38,000$ y $s_B = \$6,500$.
		\item Las estaturas y pesos de un grupo antropométrico: estatura media $170$ cm con $s = 8$ cm; peso medio $70$ kg con $s = 10$ kg.
	\end{itemize}
\end{problema}


\subsection{Soluciones detalladas}

\subsubsection*{1. Nivel Fundamental (Sencillos / Directos)}

\begin{solucion}
	\begin{enumerate}
		\item Para el arreglo $12,6,7,3,15,10,18,5$:
		\begin{itemize}
			\item \textbf{Rango:} $\max - \min = 18 - 3 = 15$
			\item \textbf{Media:} $\bar{X} = \frac{12+6+7+3+15+10+18+5}{8} = \frac{76}{8} = 9.5$
			\item \textbf{Desviación media ($DM$):}
			\begin{align}
				DM &= \frac{|12-9.5| + |6-9.5| + |7-9.5| + |3-9.5| + |15-9.5| + |10-9.5| + |18-9.5| + |5-9.5|}{8} \\
				&= \frac{2.5 + 3.5 + 2.5 + 6.5 + 5.5 + 0.5 + 8.5 + 4.5}{8} = \frac{34}{8} = 4.25
			\end{align}
			\item \textbf{Desviación estándar ($s$):}
			\begin{align}
				s &= \sqrt{\frac{(12-9.5)^2 + (6-9.5)^2 + (7-9.5)^2 + (3-9.5)^2 + (15-9.5)^2 + (10-9.5)^2 + (18-9.5)^2 + (5-9.5)^2}{8}} \\
				&= \sqrt{\frac{6.25 + 12.25 + 6.25 + 42.25 + 30.25 + 0.25 + 72.25 + 20.25}{8}} = \sqrt{\frac{190}{8}} = \sqrt{23.75} \approx 4.87
			\end{align}
		\end{itemize}
		
		\item Para el arreglo $9,3,8,8,9,8,9,18$:
		\begin{itemize}
			\item \textbf{Rango:} $18 - 3 = 15$
			\item \textbf{Media:} $\bar{X} = \frac{9+3+8+8+9+8+9+18}{8} = \frac{72}{8} = 9$
			\item \textbf{Desviación media ($DM$):}
			\begin{align}
				DM &= \frac{|9-9| + |3-9| + |8-9| + |8-9| + |9-9| + |8-9| + |9-9| + |18-9|}{8} \\
				&= \frac{0 + 6 + 1 + 1 + 0 + 1 + 0 + 9}{8} = \frac{18}{8} = 2.25
			\end{align}
			\item \textbf{Desviación estándar ($s$):}
			\begin{align}
				s &= \sqrt{\frac{0^2 + (-6)^2 + (-1)^2 + (-1)^2 + 0^2 + (-1)^2 + 0^2 + 9^2}{8}} = \sqrt{\frac{120}{8}} = \sqrt{15} \approx 3.87
			\end{align}
		\end{itemize}
	\end{enumerate}
\end{solucion}

\begin{lstlisting}[language=Python]
import numpy as np
# Conjunto 1
data1 = np.array([12, 6, 7, 3, 15, 10, 18, 5])
print("Conjunto 1:")
print(f"Rango: {np.ptp(data1)}")
print(f"Media: {np.mean(data1)}")
print(f"Desviación media: {np.mean(np.abs(data1 - np.mean(data1)))}")
print(f"Desviación estándar: {np.std(data1)}")
# Conjunto 2
data2 = np.array([9, 3, 8, 8, 9, 8, 9, 18])
print("\nConjunto 2:")
print(f"Rango: {np.ptp(data2)}")
print(f"Media: {np.mean(data2)}")
print(f"Desviación media: {np.mean(np.abs(data2 - np.mean(data2)))}")
print(f"Desviación estándar: {np.std(data2)}")
\end{lstlisting}

\begin{solucion}
	Para la tabla \ref{tab:1.4.1} que agrupa 100 estaturas estudiantiles, calculamos las marcas de clase $X_{i}$ y su media $\bar{X} = \frac{\sum f_{i}X_{i}}{N}$.
	Una vez obtenida la media del grupo ($\bar{X} \approx 67.45$ pulgadas), calculamos las desviaciones absolutas ponderadas y cuadráticas ponderadas:
	\begin{align}
		DM &= \dfrac{\sum f_{i}|X_{i} - \bar{X}|}{N} \\
		s &= \sqrt{\dfrac{\sum f_{i}(X_{i} - \bar{X})^{2}}{N}}
	\end{align}
	Al evaluar los sumatorios con las frecuencias $f_{i}$ del intervalo, obtenemos una desviación media cercana a $2.26$ pulgadas y una desviación estándar $s \approx 2.92$ pulgadas.
\end{solucion}

\begin{solucion}
	Para la tabla \ref{tab:1.4.2}, procedemos de manera análoga al cálculo de dispersión con marcas de clase.
	Calculamos los productos $f_{i}X_{i}$ y las diferencias $|X_{i} - \bar{X}|$:
	\begin{align}
		DM &= \dfrac{\sum f_{i}|X_{i} - \bar{X}|}{\sum f_{i}} \\
		s &= \sqrt{\dfrac{\sum f_{i}X_{i}^{2}}{N} - \bar{X}^{2}}
	\end{align}
	Estas medidas cuantifican la dispersión absoluta promedio respecto a la estatura central de la muestra.
\end{solucion}

\subsubsection*{2. Nivel Operativo (Intermedios / De Desarrollo)}

\begin{solucion}
	Aplicando la fórmula de cálculo abreviado (sin restar la media en cada término anterior al cuadrado):
	\begin{align}
		s = \sqrt{\dfrac{\sum f_{i}X_{i}^{2}}{N} - \left( \dfrac{\sum f_{i}X_{i}}{N} \right)^{2}}.
	\end{align}
	Calculamos cada columna de la tabla de frecuencias: $X_{i}$, $f_{i}$, $f_{i}X_{i}$ y $f_{i}X_{i}^{2}$.
	Al sustituir la suma total $\sum f_{i}X_{i}^{2}$ dividida entre $N=100$, menos el cuadrado de la media del grupo, obtenemos el mismo valor exacto de la desviación estándar que por el método de definición, evitando redondeos intermedios.
\end{solucion}

\begin{solucion}
	Para los datos distribuidos en la tabla \ref{tab:1.4.3}:
	\begin{enumerate}
		\item Determinamos las marcas de clase $X_{i}$ y las frecuencias $f_{i}$.
		\item La media aritmética es:
		\begin{align}
			\bar{X} = \dfrac{\sum f_{i}X_{i}}{N}.
		\end{align}
		\item La desviación estándar, usando la identidad operacional de momentos no centrales, es:
		\begin{align}
			s = \sqrt{\dfrac{\sum f_{i}X_{i}^{2}}{N} - \bar{X}^{2}}.
		\end{align}
	\end{enumerate}
	El resultado proporciona el centro y el radio de dispersión característico de la distribución en la tabla.
\end{solucion}

\begin{solucion}
	Como ambos grupos tienen la misma media ($\bar{X}_1 = \bar{X}_2 = 75$), la media combinada es también $\bar{X} = 75$. En este caso sin diferencia entre medias de grupo, la varianza combinada es el promedio ponderado de las varianzas individuales:
	\begin{align}
		s^2 &= \frac{n_1 s_1^2 + n_2 s_2^2}{n_1 + n_2} = \frac{30 \cdot 8^2 + 20 \cdot 12^2}{30 + 20} \\
		&= \frac{30 \cdot 64 + 20 \cdot 144}{50} = \frac{1920 + 2880}{50} = \frac{4800}{50} = 96
	\end{align}
	Por lo tanto, la desviación estándar del grupo combinado es:
	\begin{align}
		s = \sqrt{96} \approx 9.80 \text{ puntos.}
	\end{align}
\end{solucion}

\subsubsection*{3. Nivel Analítico (Avanzados / De Conexión)}

\begin{solucion}
	Partimos de la definición formal de varianza muestral (o poblacional dividida entre $N$):
	\begin{align}
		s^2 &= \frac{\sum (X_j - \bar{X})^2}{N} = \frac{\sum (X_j^2 - 2X_j\bar{X} + \bar{X}^2)}{N} \\
		&= \frac{\sum X_j^2 - 2\bar{X}\sum X_j + N\bar{X}^2}{N} = \frac{\sum X_j^2}{N} - 2\bar{X}\frac{\sum X_j}{N} + \bar{X}^2 \\
		&= \frac{\sum X_j^2}{N} - 2\bar{X}\cdot\bar{X} + \bar{X}^2 = \frac{\sum X_j^2}{N} - \bar{X}^2 = \overline{X^2} - \overline{X}^2.
	\end{align}
	Tomando la raíz cuadrada a ambos lados, se demuestra la identidad:
	\begin{align}
		s = \sqrt{\overline{X^2} - \overline{X}^2}.
	\end{align}
	Para datos con frecuencias, el mismo razonamiento algebraico es válido al reemplazar $\sum X_j^2$ por $\sum f_j X_j^2$ y $\sum X_j$ por $\sum f_j X_j$.
\end{solucion}

\begin{solucion}
	Si $d_j = X_j - P$, entonces $X_j = d_j + P$. La media de $X$ en términos del pivote es:
	\begin{align}
		\bar{X} = \frac{\sum f_j X_j}{N} = \frac{\sum f_j (d_j + P)}{N} = \frac{\sum f_j d_j}{N} + P = \bar{d} + P.
	\end{align}
	La desviación respecto a la media se transforma según:
	\begin{align}
		X_j - \bar{X} = (d_j + P) - (\bar{d} + P) = d_j - \bar{d}.
	\end{align}
	Sustituyendo en la varianza:
	\begin{align}
		s^2 &= \frac{\sum f_j (X_j - \bar{X})^2}{N} = \frac{\sum f_j (d_j - \bar{d})^2}{N} = \frac{\sum f_j (d_j^2 - 2d_j\bar{d} + \bar{d}^2)}{N} \\
		&= \frac{\sum f_j d_j^2}{N} - 2\bar{d}\frac{\sum f_j d_j}{N} + \bar{d}^2 = \frac{\sum f_j d_j^2}{N} - \bar{d}^2 = \frac{\sum f_j d_j^2}{N} - \left(\frac{\sum f_j d_j}{N}\right)^2.
	\end{align}
	Tomando raíz cuadrada, obtenemos la fórmula del pivote.
\end{solucion}

\subsubsection*{4. Nivel Desafiante (Expertos / Deducción Profunda)}

\begin{solucion}
	Sea el cambio de escala codificado $u_{i} = \dfrac{X_{i}-P}{c}$, de modo que $X_{i} = c \cdot u_{i} + P$.
	La media cumple la relación lineal $\bar{X} = c \cdot \bar{u} + P$.
	
	Restando ambas ecuaciones:
	\begin{align}
		X_{i} - \bar{X} = c \cdot (u_{i} - \bar{u}).
	\end{align}
	Al elevar al cuadrado, multiplicar por las frecuencias $f_{i}$ y sumar:
	\begin{align}
		s^{2} = \dfrac{\sum f_{i}(X_{i} - \bar{X})^{2}}{N} = \dfrac{\sum f_{i}[c(u_{i} - \bar{u})]^{2}}{N} = c^{2} \cdot \dfrac{\sum f_{i}(u_{i} - \bar{u})^{2}}{N} = c^{2} \cdot s_{u}^{2}.
	\end{align}
	Por lo tanto, al tomar la raíz cuadrada positiva:
	\begin{align}
		s = c \cdot s_{u} = c \cdot \sqrt{\dfrac{\sum f_{i}u_{i}^{2}}{N} - \left( \dfrac{\sum f_{i}u_{i}}{N} \right)^{2}}.
	\end{align}
	Aplicando esta fórmula a la tabla \ref{tab:1.4.2} (con el intervalo central como pivote $u_{i}=0$), los productos $u_{i}^{2}$ son números enteros pequeños, lo cual permite obtener $s$ de manera directa sin operar con números extensos.
\end{solucion}

\begin{solucion}
	\begin{itemize}
		\item \textbf{Salarios:}
		\begin{align}
			CV_A &= \frac{8000}{45000} \times 100\% \approx 17.78\% \\
			CV_B &= \frac{6500}{38000} \times 100\% \approx 17.11\%
		\end{align}
		Ambas empresas muestran una dispersión salarial relativa prácticamente similar (alrededor del 17\%), aunque la Empresa A tiene una variabilidad ligeramente mayor en relación con el monto de su nómina promedio.
		
		\item \textbf{Estaturas vs pesos:}
		\begin{align}
			CV_{\text{estatura}} &= \frac{8}{170} \times 100\% \approx 4.71\% \\
			CV_{\text{peso}} &= \frac{10}{70} \times 100\% \approx 14.29\%
		\end{align}
		A pesar de que las unidades físicas son distintas (cm vs kg), el coeficiente de variación permite su comparación adimensional directa. Se concluye que el peso presenta el triple de variabilidad relativa que la estatura en dicha población.
	\end{itemize}
\end{solucion}
